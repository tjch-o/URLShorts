{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "updateFrontend": "cd frontend && npm install",
    "updateBackend": "cd backend && npm install",
    "update": "concurrently \"npm run updateFrontend\" \"npm run updateBackend\"",
    "startFrontend": "cd frontend && npm start",
    "startBackend": "cd backend && node server.js",
    "startApp": "npm install && concurrently \"npm run startFrontend\" \"npm run startBackend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
